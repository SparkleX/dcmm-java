{
	"findByParent": {
		"sql": "select * from ORDR1 where ParentId = #{parentId}",
		"returnType": "List<ORDR1Model>",
		"params": {
			"parentId": "String"
		}
	},
	"deleteByParent": {
		"sql": "delete * from ORDR1 where ParentId = #{parentId}",
		"returnType": "int",
		"params": {
			"parentId": "String"
		}	
	}
}